version: 0.2

phases:
  install:
    commands:
      - echo $IMAGE_TAG
  pre_build:
    commands:
      - echo pre_build 
  build:
    commands:
      - echo build
  post_build:
    commands:
      - echo Build test2 completed on `date`

